# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.staging
.env.staging.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log

# Internal docs (not for public repo)
docs/

# Email preview output
functions/email-previews/

# Avatar generation pipeline intermediates — only the cleaned SVGs that
# land in src/services/avatar/parts.ts belong in the repo.
tools/avatars/raw/
tools/avatars/traced/
tools/avatars/clean/
.vercel

# Playwright demo intermediates — only the converted .mp4s in e2e/videos/
# are tracked. test-results/ holds raw .webm captures + per-test folders
# that the reporter consumes and discards.
test-results/
.features-gen/

# Screenshot harness outputs and saved auth states (never commit storage states).
tools/screenshot-lms/out/
tools/screenshot-lms/.auth/
tools/screenshot-lms/.state/

guide.md

# Recovered VS Code Copilot Chat history — local reference only, never commit.
.chat-archive/

# Downloaded social media captures — scraped Instagram/Threads/LinkedIn
# assets and the scripts/manifests that fetch them, for research reference
# only, not for the public repo. Cookies used to fetch these also live in
# download*.py in this folder — never commit those either. This also
# covers social-media-context.md, which names real (in some cases minor)
# individuals scraped from LinkedIn — never commit that either.
tools/social/instagram-posts/
tools/social/linkedin-posts/
tools/social/threads-posts/
tools/social/*.png
tools/social/social-media-context.md

# Performance audit artifacts (local only)
lighthouse-report*.json
lighthouse-report_*.json
lighthouse-report-abs*.json
lighthouse-report-fin*.json
lighthouse-report-final*.json
lighthouse-report-flex*.json
lighthouse-report-simple*.json
lighthouse-report-vfinal*.json
staija-perf-trace*.json.gz

# Generated at build time
public/robots.txt
public/sitemap.xml

# Generated raster export (keep source .svg)
public/staija-gradient-wordmark.png

# Marketing assets — hosted on CDN, not committed to repo
public/marketing/
public/qr-code_staija_get-involved.png

# Marketing assets — hosted on CDN, not committed to repo
public/marketing/
public/qr-code_staija_get-involved.png